Implement the CSV export feature for reports exactly as specified in
`docs/REPORT_EXPORT_SPEC.md`, matching the column order and formatting rules
described there.

Produce the work and record it following this repository's workflow.
