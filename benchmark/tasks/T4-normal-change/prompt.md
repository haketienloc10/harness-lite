The human-facing `README.md` documents the two documentation flows (the agent
flow `_harness/` and the human flow `docs/`).

Add a short, accurate subsection to `README.md` documenting that the repository
now also contains a `benchmark/` directory used to score the workflow, with a
one-line note on how to run it. Keep it concise and consistent with the existing
style.

This is a normal documentation change — not a trivial one-word fix, and not a
behavior or architecture change.
